# Publishing status

No publishing was attempted. This fixture intentionally stops at `awaiting_human_approval`.

After approval and staging integration, `result.json` must conform to `schemas/publish-result.schema.json`. Playwright may be used for browser-level validation when the WordPress API or MCP cannot confirm rendered content, robots directives, or access protection.
